var csv = require('../csv')

var columnArray = [null];
var firstNames = [];
var lastNames = [];
var addresses = [];

cvsToArray function(csv){
  csv.split('');
}

csvToAray(columnArray);
    for (i=1; i<columnArray.length, i++){
          if(i%3===0){
            csvToArray[i].push(addresses);
          }else if(i%2===0){
            csvToArray[i].push(lastNames);
          }else{
            csvToArray[i].push(firstNames);
          }
}

join()
